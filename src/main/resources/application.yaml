server:
  port: 8080
spring:
  application:
    name: app
  data:
    mongodb:
      uri: mongodb://localhost:27017/partner
  datasource:
    driver-class-name: mongodb.jdbc.MongoDriver
springdoc:
  swagger-ui:
    path: "/partner/swagger-ui.html"
    operations-sorter: "method"
